#!/bin/sh

[ -f /etc/config/classifi ] && exit 0

touch /etc/config/classifi

uci -q batch <<-EOF
	set classifi.config='classifi'
	set classifi.config.interface='br-lan'
	set classifi.config.enabled='1'
EOF

exit 0
